import {
  StyleSheet,
} from 'react-native'

export default StyleSheet.create({
  container: {
    backgroundColor: '#ecf0f1',
  },
  imageOverlay: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
    top: 0,
  },
  centerSection: {
    justifyContent: 'center',
    alignItems: 'center',
  }
});